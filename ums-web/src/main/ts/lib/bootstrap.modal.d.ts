///<reference path="jquery.d.ts" />

interface settings {
}

interface JQuery {
    modal:Function;
}
interface JQueryStatic {
    format(template: string, ...arguments: string[]): string;
    modal: any;
}