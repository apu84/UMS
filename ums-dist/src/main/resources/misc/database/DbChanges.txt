Select PR_TOKEN,TOKEN_EXP_DATE,
CASE WHEN TOKEN_EXP_DATE IS NULL THEN 'INVALID'
     WHEN TOKEN_EXP_DATE>=SYSDATE THEN 'INVALID'
          ELSE 'VALID'
     END AS "STATUS"
 from USERS Where User_Id='dpregistrar'